---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="TermAway - Your Mac Terminal, On Your iPad & iPhone"
  description="Access your Mac's terminal from your iPad or iPhone. LAN-only, no cloud, no tracking. Run Claude Code, git, docker, npm - your full dev environment, in your hands."
>
    <Header />

    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <div class="badge">Open Source & Free</div>
                    <h2 class="hero-title">Your Mac terminal,<br><span class="gradient">on your iPad & iPhone</span></h2>
                    <p class="hero-description">
                        Access your terminal from anywhere on your local network.
                        <strong>LAN-only by design</strong> — your terminal never leaves your network.
                        No cloud. No accounts. No tracking.
                    </p>
                    <div class="hero-buttons">
                        <a href="#download" class="btn btn-primary">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                                <polyline points="7 10 12 15 17 10"></polyline>
                                <line x1="12" y1="15" x2="12" y2="3"></line>
                            </svg>
                            Download for Mac
                        </a>
                        <a href="https://github.com/alexkerber/termaway" class="btn btn-glass" target="_blank">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                            </svg>
                            View on GitHub
                        </a>
                    </div>
                </div>
                <div class="hero-visual">
                    <div class="device-mockup">
                        <div class="terminal-window">
                            <div class="terminal-header">
                                <span class="terminal-dot red"></span>
                                <span class="terminal-dot yellow"></span>
                                <span class="terminal-dot green"></span>
                                <span class="terminal-title">termaway — zsh</span>
                            </div>
                            <div class="terminal-body">
                                <div class="terminal-line">
                                    <span class="terminal-prompt">~</span>
                                    <span class="terminal-command">claude</span>
                                </div>
                                <div class="terminal-output" style="margin: 12px 0;">
                                    <span style="color: var(--accent-warm);">╭─────────────────────────────────╮</span><br>
                                    <span style="color: var(--accent-warm);">│</span> <span style="color: var(--accent-cyan);">Claude Code</span> <span id="claude-version" style="color: var(--terminal-text-secondary);">v1.0.33</span> <span style="color: var(--accent-warm);">          │</span><br>
                                    <span style="color: var(--accent-warm);">╰─────────────────────────────────╯</span>
                                </div>
                                <div class="terminal-line" style="margin-top: 16px;">
                                    <span class="terminal-prompt">~/projects</span>
                                    <span class="terminal-command">bun run dev</span>
                                </div>
                                <div class="terminal-output" style="margin: 8px 0; color: var(--accent-green);">
                                    ✓ Server running at localhost:3000
                                </div>
                                <div class="terminal-line" style="margin-top: 12px;">
                                    <span class="terminal-prompt">~/projects</span>
                                    <span class="cursor"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="features" id="features">
        <div class="container">
            <div class="section-header">
                <h2>Not just AI chat. Full terminal.</h2>
                <p>Run anything — Claude Code, git, docker, npm, vim, ssh. Your entire dev environment, in your hands.</p>
            </div>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
                    </div>
                    <h3>Multi-Project Workflow</h3>
                    <p>Frontend in one tab, backend in another, infra in a third. Manage complex projects with multiple sessions.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                    </div>
                    <h3>LAN-Only Security</h3>
                    <p>Your terminal never leaves your network. No cloud servers, no accounts, no data collection.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/></svg>
                    </div>
                    <h3>Real Terminal Access</h3>
                    <p>Not a wrapper. Full PTY with proper shell, environment variables, and tool support.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><rect x="4" y="2" width="16" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg>
                    </div>
                    <h3>Touch Optimized</h3>
                    <p>Special toolbar with Ctrl, Alt, Esc, Tab, and arrow keys. Built for touch.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
                    </div>
                    <h3>Zero Config</h3>
                    <p>Bonjour auto-discovery finds your Mac instantly. Install, connect, code.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/><circle cx="12" cy="12" r="4"/></svg>
                    </div>
                    <h3>Native Performance</h3>
                    <p>SwiftTerm-powered with GPU rendering. Smooth scrolling, proper colors, feels native.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="use-cases" id="use-cases">
        <div class="container">
            <div class="use-case-card">
                <div class="use-case-content">
                    <h2>For devs who actually ship</h2>
                    <p>
                        You maintain real projects. Multiple repos, different branches, various environments.
                        Your Mac is your workstation — TermAway lets you use it from anywhere.
                    </p>
                    <ul class="use-case-list">
                        <li>Run Claude Code on your frontend while checking backend logs</li>
                        <li>Spin up docker, run tests, deploy — all from your iPad</li>
                        <li>Switch between feature branches without context switching</li>
                        <li>Your full dev environment, not a dumbed-down mobile version</li>
                    </ul>
                </div>
                <div class="emoji-grid">
                    <div class="emoji-card">
                        <span class="emoji"><svg viewBox="0 0 24 24"><path d="M20 9V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v3"/><path d="M2 11v5a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-5a2 2 0 0 0-4 0v2H6v-2a2 2 0 0 0-4 0Z"/><path d="M4 18v2"/><path d="M20 18v2"/></svg></span>
                        <span>Couch coding</span>
                    </div>
                    <div class="emoji-card">
                        <span class="emoji"><svg viewBox="0 0 24 24"><path d="M2 4v16"/><path d="M2 8h18a2 2 0 0 1 2 2v10"/><path d="M2 17h20"/><path d="M6 8v9"/></svg></span>
                        <span>Bed debugging</span>
                    </div>
                    <div class="emoji-card">
                        <span class="emoji"><svg viewBox="0 0 24 24"><path d="M17 8h1a4 4 0 1 1 0 8h-1"/><path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4Z"/><line x1="6" y1="2" x2="6" y2="4"/><line x1="10" y1="2" x2="10" y2="4"/><line x1="14" y1="2" x2="14" y2="4"/></svg></span>
                        <span>Coffee table commits</span>
                    </div>
                    <div class="emoji-card">
                        <span class="emoji"><svg viewBox="0 0 24 24"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/></svg></span>
                        <span>Late night deploys</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="how-it-works" id="how-it-works">
        <div class="container">
            <div class="section-header">
                <h2>Up and running in minutes</h2>
                <p>Get your Mac terminal on your iPad or iPhone in three easy steps</p>
            </div>
            <div class="steps-grid">
                <div class="step-card">
                    <div class="step-number">1</div>
                    <h3>Install on your Mac</h3>
                    <p>Download the macOS menu bar app. It runs a lightweight server that your devices can connect to.</p>
                    <a href="#" class="step-link download-link-macos">Download for macOS</a>
                </div>
                <div class="step-card">
                    <div class="step-number">2</div>
                    <h3>Get the iOS app</h3>
                    <p>Install TermAway on your iPad or iPhone. It automatically finds your Mac on the network.</p>
                    <span class="step-link step-link-soon">Coming soon to App Store</span>
                </div>
                <div class="step-card">
                    <div class="step-number">3</div>
                    <h3>Start coding</h3>
                    <p>Open a session and you're in. Full terminal access with multiple tabs, just like on your Mac.</p>
                </div>
            </div>
            <p class="browser-note">
                In a hurry? You can also connect via any browser at <code>http://your-mac-ip:3000</code> — but the native apps offer a smoother, more stable experience.
            </p>
        </div>
    </section>

    <section class="download" id="download">
        <div class="container">
            <div class="download-content">
                <h2>Get TermAway</h2>
                <p class="section-subtitle">Choose your platform and start coding remotely</p>
                <div class="download-cards">
                    <div class="download-card">
                        <div class="icon"><svg viewBox="0 0 24 24"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg></div>
                        <h3>macOS</h3>
                        <p>Menu bar app that runs the terminal server</p>
                        <a href="#" class="btn btn-primary download-link-macos">
                            Download for Mac
                        </a>
                    </div>
                    <div class="download-card coming-soon">
                        <div class="icon"><svg viewBox="0 0 24 24"><rect x="4" y="2" width="16" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg></div>
                        <h3>iOS / iPadOS</h3>
                        <p>Native app with SwiftTerm</p>
                        <span class="coming-soon-badge">Coming Soon</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <Footer />

    <script is:inline>
        // Fetch latest macOS release from GitHub
        fetch('https://api.github.com/repos/alexkerber/termaway/releases/latest')
            .then(res => res.json())
            .then(release => {
                const asset = release.assets.find(a => a.name.includes('macOS'));
                if (asset) {
                    document.querySelectorAll('.download-link-macos').forEach(el => {
                        el.href = asset.browser_download_url;
                    });
                }
            })
            .catch(() => {
                document.querySelectorAll('.download-link-macos').forEach(el => {
                    el.href = 'https://github.com/alexkerber/termaway/releases';
                });
            });

        // Fetch latest Claude Code version for the terminal demo
        fetch('https://api.github.com/repos/anthropics/claude-code/releases/latest')
            .then(res => res.json())
            .then(release => {
                const versionEl = document.getElementById('claude-version');
                if (versionEl && release.tag_name) {
                    versionEl.textContent = release.tag_name;
                }
            })
            .catch(() => {});
    </script>
</Layout>

<style>
    /* Hero Section */
    .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: 120px 0 80px;
    }

    .hero-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 60px;
        align-items: center;
    }

    .hero-text {
        max-width: 560px;
    }

    .badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 16px;
        background: var(--bg-glass);
        border: 1px solid var(--border-glass);
        border-radius: 50px;
        font-size: 0.85rem;
        color: #FFB627;
        margin-bottom: 24px;
        backdrop-filter: blur(10px);
    }

    :global([data-theme="light"]) .badge {
        color: #E55A2B;
    }

    @media (prefers-color-scheme: light) {
        :global(:root:not([data-theme])) .badge {
            color: #E55A2B;
        }
    }

    .badge::before {
        content: '';
        width: 8px;
        height: 8px;
        background: #FFB627;
        border-radius: 50%;
        animation: pulse 2s infinite;
    }

    :global([data-theme="light"]) .badge::before {
        background: #E55A2B;
    }

    @media (prefers-color-scheme: light) {
        :global(:root:not([data-theme])) .badge::before {
            background: #E55A2B;
        }
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }

    .hero-title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: 24px;
    }

    .hero-title .gradient {
        background: linear-gradient(90deg, #FFB627, #FF6B35);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    :global([data-theme="light"]) .hero-title .gradient {
        background: linear-gradient(90deg, #E55A2B, #C44B1C);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    @media (prefers-color-scheme: light) {
        :global(:root:not([data-theme])) .hero-title .gradient {
            background: linear-gradient(90deg, #E55A2B, #C44B1C);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
    }

    .hero-description {
        font-size: 1.25rem;
        color: var(--text-secondary);
        margin-bottom: 40px;
        line-height: 1.7;
    }

    .hero-description strong {
        color: var(--text-primary);
    }

    .hero-buttons {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
    }

    .hero-visual {
        position: relative;
    }

    .device-mockup {
        position: relative;
        background: var(--device-bg);
        border: 1px solid var(--border-glass);
        border-radius: 24px;
        padding: 20px;
        backdrop-filter: blur(20px);
        box-shadow: var(--device-shadow);
    }

    .terminal-window {
        background: var(--terminal-bg);
        border-radius: 12px;
        overflow: hidden;
    }

    .terminal-header {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 12px 16px;
        background: var(--terminal-header-bg);
    }

    .terminal-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
    }

    .terminal-dot.red { background: #ff5f56; }
    .terminal-dot.yellow { background: #ffbd2e; }
    .terminal-dot.green { background: #27ca40; }

    .terminal-title {
        flex: 1;
        text-align: center;
        font-size: 0.85rem;
        color: var(--terminal-text-secondary);
    }

    .terminal-body {
        padding: 20px;
        font-family: 'Geist Mono', 'SF Mono', 'Menlo', monospace;
        font-size: 0.9rem;
        line-height: 1.8;
        min-height: 300px;
    }

    .terminal-line {
        display: flex;
        gap: 8px;
    }

    .terminal-prompt {
        color: var(--accent-green);
    }

    .terminal-command {
        color: var(--terminal-text);
    }

    .terminal-output {
        color: var(--terminal-text-secondary);
    }

    .cursor {
        display: inline-block;
        width: 10px;
        height: 20px;
        background: var(--accent-orange);
        animation: blink 1s infinite;
        vertical-align: middle;
        margin-left: 2px;
    }

    @keyframes blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
    }

    /* Features Section */
    .features {
        padding: 120px 0;
    }

    .section-header {
        text-align: center;
        max-width: 600px;
        margin: 0 auto 60px;
    }

    .section-header h2 {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 16px;
        line-height: 1.2;
    }

    .section-header p {
        color: var(--text-secondary);
        font-size: 1.1rem;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
    }

    .feature-card {
        background: var(--bg-glass);
        border: 1px solid var(--border-glass);
        border-radius: 20px;
        padding: 32px;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

    .feature-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .feature-icon {
        width: 48px;
        height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        margin-bottom: 20px;
        background: rgba(255, 182, 39, 0.1);
    }

    .feature-icon svg {
        width: 24px;
        height: 24px;
        stroke: #FFB627;
        stroke-width: 1.5;
        fill: none;
    }

    .feature-card h3 {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 12px;
    }

    .feature-card p {
        color: var(--text-secondary);
        font-size: 0.95rem;
    }

    /* Use Cases Section */
    .use-cases {
        padding: 120px 0;
    }

    .use-case-card {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 60px;
        align-items: center;
        padding: 60px;
        background: var(--bg-glass);
        border: 1px solid var(--border-glass);
        border-radius: 32px;
        backdrop-filter: blur(20px);
    }

    .use-case-content h2 {
        font-size: 2rem;
        margin-bottom: 20px;
    }

    .use-case-content p {
        color: var(--text-secondary);
        font-size: 1.1rem;
        margin-bottom: 24px;
    }

    .use-case-list {
        list-style: none;
    }

    .use-case-list li {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px 0;
        color: var(--text-secondary);
    }

    .use-case-list li::before {
        content: '✓';
        color: var(--accent-green);
        font-weight: 700;
    }

    .emoji-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
    }

    .emoji-card {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid var(--border-glass);
        border-radius: 20px;
        padding: 32px;
        text-align: center;
        transition: transform 0.3s;
    }

    .emoji-card:hover {
        transform: scale(1.05);
    }

    .emoji-card .emoji {
        margin-bottom: 12px;
        display: block;
    }

    .emoji-card .emoji svg {
        width: 40px;
        height: 40px;
        stroke: #FFB627;
        stroke-width: 1.5;
        fill: none;
    }

    .emoji-card span:last-child {
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    /* How It Works Section */
    .how-it-works {
        padding: 120px 0;
    }

    .steps-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 32px;
        margin-top: 60px;
    }

    .step-card {
        text-align: center;
        position: relative;
    }

    .step-number {
        width: 56px;
        height: 56px;
        background: linear-gradient(135deg, #FFB627, #FF6B35);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 24px;
        font-size: 1.5rem;
        font-weight: 700;
        color: white;
    }

    .step-card h3 {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 12px;
    }

    .step-card p {
        color: var(--text-secondary);
        font-size: 0.95rem;
        line-height: 1.6;
    }

    .step-link {
        display: inline-block;
        margin-top: 16px;
        color: var(--accent-orange);
        text-decoration: none;
        font-weight: 500;
        font-size: 0.9rem;
    }

    .step-link:hover {
        text-decoration: underline;
    }

    .step-link-soon {
        color: var(--text-secondary);
        opacity: 0.7;
    }

    .step-link-soon:hover {
        text-decoration: none;
    }

    .step-card:not(:last-child)::after {
        content: '';
        position: absolute;
        top: 26px;
        left: calc(50% + 40px);
        width: calc(100% - 80px);
        height: 2px;
        background: var(--border-glass);
    }

    .browser-note {
        color: var(--text-secondary);
        font-size: 0.95rem;
        margin-top: 48px;
        text-align: center;
    }

    .browser-note code {
        background: var(--bg-glass);
        padding: 4px 8px;
        border-radius: 4px;
    }

    /* Download Section */
    .download {
        padding: 120px 0;
    }

    .download-content {
        text-align: center;
        max-width: 800px;
        margin: 0 auto;
    }

    .download h2 {
        font-size: 2.5rem;
        margin-bottom: 16px;
    }

    .download-content > .section-subtitle {
        color: var(--text-secondary);
        font-size: 1.1rem;
        margin-bottom: 48px;
    }

    .download-cards {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
        margin-bottom: 48px;
        align-items: stretch;
    }

    .download-card {
        background: var(--bg-glass);
        border: 1px solid var(--border-glass);
        border-radius: 24px;
        padding: 40px;
        text-align: center;
        backdrop-filter: blur(10px);
        transition: all 0.3s;
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    .download-card:hover {
        border-color: var(--accent-orange);
        box-shadow: 0 0 40px var(--glow-orange);
    }

    .download-card.coming-soon {
        opacity: 0.7;
    }

    .download-card .icon {
        margin-bottom: 20px;
    }

    .download-card .icon svg {
        width: 56px;
        height: 56px;
        stroke: #FFB627;
        stroke-width: 1.5;
        fill: none;
    }

    .download-card h3 {
        font-size: 1.5rem;
        margin-bottom: 8px;
    }

    .download-card p {
        color: var(--text-secondary);
        margin-bottom: 0;
        flex: 1;
    }

    .download-card .btn,
    .download-card .coming-soon-badge {
        width: 100%;
        justify-content: center;
        margin-top: 24px;
    }

    .coming-soon-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        padding: 16px 24px;
        background: var(--accent-orange);
        color: white;
        border: none;
        border-radius: 50px;
        font-size: 1rem;
        font-weight: 600;
        opacity: 0.4;
    }

    /* Responsive */
    @media (max-width: 1024px) {
        .hero-content {
            grid-template-columns: 1fr;
            text-align: center;
        }

        .hero-text {
            max-width: 100%;
        }

        .hero-buttons {
            justify-content: center;
        }

        .hero-visual {
            max-width: 500px;
            margin: 0 auto;
        }

        .features-grid {
            grid-template-columns: repeat(2, 1fr);
        }

        .use-case-card {
            grid-template-columns: 1fr;
        }

        .download-cards {
            grid-template-columns: 1fr;
            max-width: 400px;
            margin-left: auto;
            margin-right: auto;
        }

        .steps-grid {
            grid-template-columns: 1fr;
            gap: 48px;
            max-width: 400px;
            margin-left: auto;
            margin-right: auto;
        }

        .step-card:not(:last-child)::after {
            display: none;
        }
    }

    @media (max-width: 640px) {
        .hero-title {
            font-size: 1.75rem;
        }

        .hero-description {
            font-size: 1rem;
        }

        .hero {
            min-height: auto;
            padding: 100px 0 40px;
        }

        .hero-buttons {
            flex-direction: column;
            align-items: center;
        }

        .hero-buttons .btn-glass {
            background: none;
            border: none;
            padding: 12px 0;
            font-size: 0.95rem;
            color: var(--accent-orange);
        }

        .hero-buttons .btn-glass:hover {
            background: none;
        }

        .hero-buttons .btn-glass svg {
            display: none;
        }

        .hero-visual {
            display: none;
        }

        .section-header {
            margin-bottom: 32px;
            padding: 0 20px;
        }

        .section-header h2 {
            font-size: 1.5rem;
        }

        .section-header p {
            font-size: 0.95rem;
        }

        .features {
            padding: 60px 0;
        }

        .features .container {
            padding: 0;
        }

        .features-grid {
            display: flex;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            -webkit-overflow-scrolling: touch;
            gap: 16px;
            padding: 16px 20px 24px 20px;
        }

        .features-grid::-webkit-scrollbar {
            display: none;
        }

        .feature-card {
            flex: 0 0 280px;
            scroll-snap-align: start;
            padding: 24px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .feature-card:hover {
            transform: none;
        }

        .feature-card h3 {
            font-size: 1.1rem;
        }

        .feature-card p {
            font-size: 0.85rem;
        }

        .use-cases {
            padding: 60px 0;
        }

        .use-case-card {
            padding: 20px;
        }

        .use-case-content h2 {
            font-size: 1.5rem;
        }

        .use-case-content p {
            font-size: 0.95rem;
        }

        .emoji-grid {
            gap: 12px;
        }

        .emoji-card {
            padding: 16px;
        }

        .emoji-card .emoji svg {
            width: 32px;
            height: 32px;
        }

        .how-it-works {
            padding: 60px 0;
        }

        .steps-grid {
            margin-top: 32px;
            gap: 40px;
        }

        .step-number {
            width: 48px;
            height: 48px;
            font-size: 1.25rem;
        }

        .download {
            padding: 60px 0;
        }

        .download h2 {
            font-size: 1.75rem;
        }

        .download-card {
            padding: 24px;
        }
    }
</style>
